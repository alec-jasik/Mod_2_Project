<!DOCTYPE html>
<html lang="en">
<head>
<title>CSS Template</title>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<link rel="preconnect" href="https://fonts.gstatic.com">
<link href="https://fonts.googleapis.com/css2?family=Source+Sans+Pro:wght@900&display=swap" rel="stylesheet">

<style>
* {
  box-sizing: border-box;
 font-family: 'Source Sans Pro', sans-serif;
}

body {
  margin: 0;
  
  position: relative;
  background: url("https://www.incimages.com/uploaded_files/image/1920x1080/getty_636342562_402329.jpg") no-repeat center center/cover;
 
  background-attachment: fixed;
  height: 100vh;
}


/* Style the top navigation bar */
.topnav {
  overflow: hidden;
 
}

/* Style the topnav links */
.topnav a {
  float: left;
  display: block;
  color: black;
  text-align: center;
  padding: 14px 16px;
  text-decoration: none;
}

/* Change color on hover */
.topnav a:hover {
  background-color: #8796C4;
  border: none;
  color: black;
  text-align: center;
  font-size: 16px;
  transition: 0.7s;
}

/* Style the content */
.content {
  padding: 10px;
}

/* unvisited link */
a:link {
  color: black;
}

/* visited link */
a:visited {
  color: black;
}



/* selected link */
a:active {
  color: black;
}

.content: a {
  font-weight: bold;
}

/* Style the footer */
.footer {
  /*background-color: #ffe8cc;*/
  padding: 10px;
}
</style>
</head>
<body>

<div class="topnav">
  <%= link_to "Home", visit_reviews_path%>
  <%= link_to "Destinations", destinations_path %>
  <%= link_to "Points of Interest", pois_path %>
  <%= link_to "Reviews", reviews_path %>
  <%= link_to "Wishlists", wishlists_path %>
  <%= link_to "Trips", trips_path %>
</div>

<div class="content">
<h1>Create New Review</h1>
<hr>
<br>
 <%= form_with model: @review do | f |%>

<%= f.label :Name %>
<%= f.collection_select :traveler_id, Traveler.all, :id, :full_name %>

<%= f.label :Visited %>
<%= f.collection_select :poi_id, Poi.all, :id, :name %>

<%= f.label :title %>
<%= f.text_field :title %>

<%= f.label :description %>
<%= f.text_field :description %>

<%= f.label :rating %>
<%= f.text_field :rating %>

<%= f.submit %>
<% end %>
</div>

</body>
</html>

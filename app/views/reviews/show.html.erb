<!DOCTYPE html>
<html lang="en">
<head>
<title>CSS Template</title>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<style>
* {
  box-sizing: border-box;
  font-family: Arial, Helvetica, sans-serif;
}

body {
  margin: 0;
  font-family: Arial, Helvetica, sans-serif;
}

/* Style the top navigation bar */
.topnav {
  overflow: hidden;
  background-color: #ffe8cc;
}

/* Style the topnav links */
.topnav a {
  float: left;
  display: block;
  color: black;
  text-align: center;
  padding: 14px 16px;
  text-decoration: none;
}

/* Change color on hover */
.topnav a:hover {
  background-color: #ddd;
  color: black;
}

/* Style the content */
.content {
  background-color: white;
  padding: 10px;
}

/* Style the footer */
.footer {
  background-color: #ffe8cc;
  padding: 10px;
}
</style>
</head>
<body>

<div class="topnav">
  <%= link_to "Home", visit_reviews_path%>
  <%= link_to "Destinations", destinations_path %>
  <%= link_to "Points of Interest", pois_path %>
  <%= link_to "Reviews", reviews_path %>
  <%= link_to "Wishlists", wishlists_path %>
  <%= link_to "Trips", trips_path %>
</div>

<div class="content">
  <h1>Review of <%= @review.poi.name %>: "<%= @review.title %>"</h1>
<%=link_to @review.poi.destination.city_name, destination_path(@review.poi.destination)%>, <%= @review.poi.destination.country_name%>
<hr>
<%= @review.description %> <br><br>
<h3>Rating (1 - 5 stars):  <%= @review.rating%></h3>
<p>Written by: <%= link_to @review.traveler.full_name, traveler_path(@review.traveler) %></p>
</div>

<div class="footer">
<%= link_to "Click here to edit this Review!", edit_review_path%>
</br>
</br>
<%= button_to "Delete Review", {:controller => :reviews, :action => 'destroy', :id => @review.id }, :method => :delete %>
</div>

</body>
</html>

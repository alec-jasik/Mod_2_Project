<!DOCTYPE html>
<html lang="en">
<head>
<title>CSS Template</title>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<style>
* {
  box-sizing: border-box;
  font-family: Arial, Helvetica, sans-serif;
}

body {
  margin: 0;
  font-family: Arial, Helvetica, sans-serif;
}

/* Style the top navigation bar */
.topnav {
  overflow: hidden;
  background-color: #ffe8cc;
}

/* Style the topnav links */
.topnav a {
  float: left;
  display: block;
  color: black;
  text-align: center;
  padding: 14px 16px;
  text-decoration: none;
}

/* Change color on hover */
.topnav a:hover {
  background-color: #ddd;
  color: black;
}

/* Style the content */
.content {
  background-color: white;
  padding: 10px;
}

/* Style the footer */
.footer {
  background-color: #ffe8cc;
  padding: 10px;
}
</style>
</head>
<body>

<div class="topnav">
  <%= link_to "Home", visit_reviews_path%>
  <%= link_to "Destinations", destinations_path %>
  <%= link_to "Points of Interest", pois_path %>
  <%= link_to "Reviews", reviews_path %>
  <%= link_to "Wishlists", wishlists_path %>
  <%= link_to "Trips", trips_path %>
</div>

<div class="content">
<h1><%= @traveler.full_name %>'s Profile</h1>
<hr>
<h4><%= @traveler.full_name %>'s Trips:</h4>
<% @traveler.trips.each do | trip |%>
<li> Has visited <%= link_to trip.destination.city_name, destination_path(trip.destination) %>,  <%= trip.destination.country_name %></li>
<% end %>
<h4><%= @traveler.full_name %>'s Wishlist:</h4>
<% @traveler.wishlists.each do | wishlist |%>
<li> Wishes to visit <%= link_to wishlist.destination.city_name, destination_path(wishlist.destination) %>,  <%= wishlist.destination.country_name %></li>
<% end %>
<h4><%= @traveler.full_name %>'s Reviews:</h4>
<% @traveler.reviews.each do | review |%>
<li> Title: <%= link_to review.title, review_path(review) %></li>
<% end %>
</div>

<div class="footer">
<%= button_to "Delete Profile", {:controller => :travelers, :action => 'destroy', :id => @traveler.id }, :method => :delete %>
</div>

</body>
</html>
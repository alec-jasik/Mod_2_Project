<!DOCTYPE html>
<html lang="en">
<head>
<title>CSS Template</title>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<style>
* {
  box-sizing: border-box;
  font-family: Arial, Helvetica, sans-serif;
}

body {
  margin: 0;
  font-family: Arial, Helvetica, sans-serif;
}

/* Style the top navigation bar */
.topnav {
  overflow: hidden;
  background-color: #ffe8cc;
}

/* Style the topnav links */
.topnav a {
  float: left;
  display: block;
  color: black;
  text-align: center;
  padding: 14px 16px;
  text-decoration: none;
}

/* Change color on hover */
.topnav a:hover {
  background-color: #ddd;
  color: black;
}

/* Style the content */
.content {
  background-color: white;
  padding: 10px;
}

/* Style the footer */
.footer {
  background-color: #ffe8cc;
  padding: 10px;
}
</style>
</head>
<body>

<div class="topnav">
  <%= link_to "Home", visit_reviews_path%>
  <%= link_to "Destinations", destinations_path %>
  <%= link_to "Points of Interest", pois_path %>
  <%= link_to "Reviews", reviews_path %>
  <%= link_to "Wishlists", wishlists_path %>
  <%= link_to "Trips", trips_path %>
</div>

<div class="content">
  <h1><%= @poi.name %></h1>

<%= link_to @poi.destination.city_name, destination_path(@poi.destination)%>, <%= @poi.destination.country_name%>


<p><%= @poi.description %></p>
<hr>
<h3>Reviews by Title (click a title to see that review):</h3>
<%@poi.reviews.each do |review| %>
<li><%= link_to review.title, review_path(review)%></li>
<% end %>
</div>

<div class="footer">
  <%= link_to "Click here to write a review!", new_review_path%>
</br>
</br>
<%= link_to "Click here to edit this Point of Interest!", edit_poi_path%>
</br>
</br>
<%= button_to "Delete POI", {:controller => :pois, :action => 'destroy', :id => @poi.id }, :method => :delete %>
</br>
</br>
</div>

</body>
</html>
